# 本机曲库与配置（机器相关，不进仓库）
local-source.toml
.music/

# `stage_media pack` 的产物（~322 MB）：主仓库那份 .gitignore 早就有，这里补上
otomads-media.tar.gz

# `fetch_covers` 的封面缓存（JSON Lines，一行一条；机器相关，可续跑，不进仓库）
.covers-cache.jsonl

# Python / uv
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.uv/

# 编辑器 / 系统
.DS_Store

# Worker 那边的本地产物（`wrangler dev` / 本地跑构建脚本时会生成；站点配置在仓库根，
# 所以这些默认落在仓库里 —— 都不是要提交的东西，D148）
.wrangler/
/dist/
